db_shaper will format an OSM file into a csv.

To run it:
-Edit OSM_FILE_NAME, DB, VALIDATE, READ_DIR, WRITE DIR to your liking.
-In the command prompt (or terminal), navigate to db_shaper. Then type 'python execute.py'